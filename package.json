{
  "name": "11auction-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix Frontend",
    "dev:test": "npm run dev:test --prefix Frontend",
    "build": "npm run build --prefix Frontend",
    "start": "npm run start --prefix Frontend",
    "lint": "npm run lint --prefix Frontend"
  }
}
